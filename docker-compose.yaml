version: '3.8'
services:
  api-oa-integrator:
    build: ./backend
    container_name: api-oa-integrator
    ports:
      - "1323:1323"
    environment:
      DATABASE_URL: postgres://postgres:postgres123@oa-integrator-db:5432/postgres?sslmode=disable
    depends_on:
      - oa-integrator-db
  oa-dashboard:
    build:
      context: ./web
      args:
        REACT_APP_API_URL: ${BACKEND_BASE_URL}
    container_name: oa-dashboard
    ports:
      - "3000:3000"
    depends_on:
      - api-oa-integrator
  oa-integrator-db:
    image: postgres:latest
    container_name: postgres_db
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres123
    ports:
      - '5432:5432'
    volumes:
      - postgres_db:/var/lib/postgresql/data
volumes:
  postgres_db:
    driver: local